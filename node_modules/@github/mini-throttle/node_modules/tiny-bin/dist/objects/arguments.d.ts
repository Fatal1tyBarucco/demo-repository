import Collection from './collection';
import type Argument from './argument';
declare class Arguments extends Collection<Argument> {
    print(mode?: 'line' | 'lines'): void;
}
export default Arguments;
