/* IMPORT */
import ini from 'ini-simple-parser';
import cast from './cast.js';
/* MAIN */
const parse = (input) => {
    return cast(ini(input, {
        inferBooleans: true,
        inferNumbers: true,
        inferStrings: true,
        inlineComments: true
    }));
};
/* EXPORT */
export default parse;
