import type Cache from "./cache.js";
import type { Options, Prettier } from "./types.js";
declare function makeCached(options: Options, cache: Cache, prettier: Prettier): Prettier;
export { makeCached };
