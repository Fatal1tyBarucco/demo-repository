import Addon from './addon';
import type Bin from './bin';
import type { OptionData, OptionOptions } from '../types';
declare class Option extends Addon {
    id: string;
    name: string;
    description: string;
    section: string;
    deprecated: boolean;
    eager: boolean;
    hidden: boolean;
    required: boolean;
    variadic: boolean;
    default: unknown;
    enum?: string[];
    data: OptionData;
    constructor(bin: Bin, options: OptionOptions);
    parse(name: string): OptionData;
}
export default Option;
