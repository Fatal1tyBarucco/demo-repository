import type { Check, Rule } from 'axe-core';
interface RuleOptions {
    id: string;
    excludeHidden?: boolean;
    selector: string;
    checks?: Array<(el: Element) => boolean>;
    all?: string[];
    any?: string[];
    help: string;
    helpUrl?: string;
    impact?: 'critical' | 'serious' | 'moderate' | 'minor' | 'critical';
}
export declare function createRule({ id, excludeHidden, selector, checks, all, any, help, helpUrl, impact, }: RuleOptions): void;
declare const _default: {
    rules: Rule[];
    checks: Check[];
};
export default _default;
