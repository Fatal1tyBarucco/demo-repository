import Addon from './addon';
import type Command from './command';
declare class Usage extends Addon {
    protected usages: Set<string>;
    print(command: Command): void;
    register(usage: string): void;
}
export default Usage;
