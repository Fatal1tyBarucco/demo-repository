import type { Dirent, Options, Result } from './types';
declare const readdirGlob: (glob: string | string[], options?: Options) => Promise<Result>;
export default readdirGlob;
export type { Dirent, Options, Result };
